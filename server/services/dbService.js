// services/dbService.js — работа с PostgreSQL (SQL-запросы)

async function getCachedAir(lat, lon, radius) {
    return null; // заглушка: «свежих данных в кэше нет»
}

module.exports = {
    getCachedAir,
};